{
  "name": "TaskList",
  "version": "1.0.0",
  "description": "A MEAN app that allows users to manage their tasks",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.13.3",
    "express": "^4.13.3",
    "mongodb": "^2.1.6",
    "socket.io": "^1.7.3",
    "websocket": "^1.0.24"
  },
  "devDependencies": {
    "@angular/cli": "^1.0.0"
  }
}
